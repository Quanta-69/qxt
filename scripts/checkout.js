//Add to cart button
/*
let cartBtn = document.querySelectorAll('.add-to-cart');
let price = 0
let nichePrice = document.querySelectorAll('.niche-price')

nichePrice.forEach(niche =>{
    niche.textContent = ""
    function test (){
        cartBtn.forEach(check =>{
            check.addEventListener('click', () =>{
                niche.textContent = `Count is ${price}`
            })
        })
    }
    test()
});

function added(){
    cartBtn.forEach(button =>{
        button.addEventListener('click', () => {
            button.textContent = `Added`
            button.classList.add('hello')
        })
    })
};


cartBtn.onclick = added()
cartBtn.onclick = test()*/